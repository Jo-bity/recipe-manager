# Separate Authoring from Command Preview

The UI should separate Recipe authoring from vendor command preview. Authoring is where a Robotics Technician adapts Recipe intent to Battery Layouts and Process Constraints; preview is where the same vendor-neutral Recipe JSON is inspected through a Robot Adapter. Keeping these as separate views makes the core workflow clearer and avoids mixing technician editing with integration review.
