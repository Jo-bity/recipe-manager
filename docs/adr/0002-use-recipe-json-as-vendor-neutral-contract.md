# Use Recipe JSON as the Vendor-Neutral Contract

Recipe JSON will be the canonical representation of technician intent. Robot Adapters translate this vendor-neutral Recipe JSON into Company A or Company B command plans, and the MVP will expose preview rather than execution. This proves the integration architecture without taking on hardware safety, credentials, retries, and operational concerns that belong outside the 6-hour MVP.
